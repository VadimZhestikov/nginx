● Here's the detailed property tree for every Nginx* class:

  ---
  NginxCycle
    .hostname                  string, r/o
    .prefix                    string, r/o — config directory
    .workers                   number, r/w
    .confFile                  string, r/o
    .errorLog                  string, r/o
    .installPrefix             string, r/o
    .pid                       string, r/o
    .connectionN               number, r/o — max connections per worker
    .daemon                    boolean, r/o

  ---
  NginxServer
    .name                      string, r/o — primary server_name
    .root                      string, r/w
    .names[]                   string[], r/o
    .locations[]               NginxLocation[]
    .clientHeaderBufferSize    number, r/o — bytes
    .clientHeaderTimeout       number, r/o — ms
    .ignoreInvalidHeaders      boolean, r/o
    .mergeSlashes              boolean, r/o
    .underscoresInHeaders      boolean, r/o
    .serverTokens              string, r/o — "on"|"off"|"build"
    .connectionPoolSize        number, r/o — bytes
    .requestPoolSize           number, r/o — bytes
    .largeClientHeaderBuffers  {number, size}, r/o
    .listen[]                  [{addr, port, ssl, http2, default}, ...]
    .ssl                       NginxSSL | null

  ---
  NginxLocation
    .path                        string, r/o
    .root                        string, r/w
    .handler                     function, r/w — JS content handler
    .alias                       string | null, r/o
    .internal                    boolean, r/o
    .sendfile / .tcpNopush / .tcpNodelay / .etag    boolean, r/o
    .keepaliveTimeout            number, r/o — ms
    .keepaliveRequests           number, r/o
    .keepaliveTime               number, r/o — ms
    .keepaliveDisable            string[], r/o
    .keepaliveMinTimeout         number, r/o — ms
    .clientMaxBodySize           number, r/o — bytes
    .clientBodyTimeout           number, r/o — ms
    .clientBodyBufferSize        number, r/o — bytes
    .clientBodyInFileOnly        string, r/o — "off"|"on"|"clean"
    .clientBodyInSingleBuffer    boolean, r/o
    .sendTimeout                 number, r/o — ms
    .sendfileMaxChunk            number, r/o — bytes
    .sendLowat                   number, r/o — bytes
    .readAhead                   number, r/o — bytes
    .postponeOutput              number, r/o — bytes
    .defaultType                 string, r/o
    .tryFiles[]                  string[], r/o
    .satisfy                     string, r/o — "all"|"any"
    .limitExcept[]               string[], r/o — restricted methods
    .lingering                   string, r/o — "off"|"on"|"always"
    .lingeringTimeout            number, r/o — ms
    .lingeringTime               number, r/o — ms
    .resolverTimeout             number, r/o — ms
    .chunkedTransferEncoding     boolean, r/o
    .resetTimedoutConnection     boolean, r/o
    .msieRefresh / .msiePadding  boolean, r/o
    .logNotFound / .logSubrequest / .recursiveErrorPages  boolean, r/o
    .absoluteRedirect            boolean, r/o
    .serverNameInRedirect        boolean, r/o
    .portInRedirect              boolean, r/o
    .ifModifiedSince             string, r/o — "off"|"exact"|"before"
    .maxRanges                   number, r/o
    .authDelay                   number, r/o — ms
    .directio                    number, r/o — bytes
    .directioAlignment           number, r/o — bytes
    .typesHashMaxSize            number, r/o
    .errorPage[]                 [{status, overwrite, uri}, ...]
    .proxy                       NginxProxy | null
    .gzip                        NginxGzip | null
    .headers                     NginxHeaders | null
    .rewrite                     NginxRewrite | null
    .access                      NginxAccess | null
    .auth                        NginxAuth | null
    .limitReq                    NginxLimitReq | null
    .limitConn                   NginxLimitConn | null
    .fastcgi                     NginxFastCGI | null
    .log                         NginxLog | null
    .realip                      NginxRealIP | null
    .charset                     NginxCharset | null
    .subFilter                   NginxSubFilter | null
    .autoindex                   NginxAutoindex | null
    .referer                     NginxReferer | null
    .dav                         NginxDav | null
    .ssi                         NginxSsi | null
    .userid                      NginxUserid | null
    .addition                    NginxAddition | null
    .gunzip                      NginxGunzip | null
    .slice                       NginxSlice | null
    .imageFilter                 NginxImageFilter | null
    .xslt                        NginxXslt | null
    .secureLink                  NginxSecureLink | null
    .mp4                         NginxMp4 | null
    .randomIndex                 NginxRandomIndex | null
    .authRequest                 NginxAuthRequest | null
    .gzipStatic                  NginxGzipStatic | null
    .memcached                   NginxMemcached | null
    .scgi                        NginxScgi | null
    .uwsgi                       NginxUwsgi | null
    .mirror                      NginxMirror | null

  ---
  NginxProxy
    .pass                    string, r/o
    .httpVersion             number, r/o — 10 | 11
    .connectTimeout          number, r/o — ms
    .sendTimeout             number, r/o — ms
    .readTimeout             number, r/o — ms
    .buffering               boolean, r/o
    .requestBuffering        boolean, r/o
    .interceptErrors         boolean, r/o
    .bufferSize              number, r/o — bytes
    .buffers[]               [{size, count}], r/o
    .nextUpstream[]          string[], r/o — fallback conditions
    .nextUpstreamTries       number, r/o
    .nextUpstreamTimeout     number, r/o — ms
    .setHeader[]             [{field, value}, ...], r/o
    .cache                   NginxProxyCache | null

  NginxProxyCache
    .zone                    string, r/o — shm zone name
    .minUses                 number, r/o
    .lock                    boolean, r/o
    .lockTimeout             number, r/o — ms
    .lockAge                 number, r/o — ms
    .revalidate              boolean, r/o
    .convertHead             boolean, r/o
    .backgroundUpdate        boolean, r/o
    .methods[]               string[], r/o
    .valid[]                 [{code, time}, ...], r/o — time in ms
    .useStale[]              string[], r/o — condition strings

  ---
  NginxSSL
    .certificate[]           string[], r/o — cert paths
    .certificateKey[]        string[], r/o — key paths
    .protocols[]             string[], r/o — e.g. ["TLSv1.2", "TLSv1.3"]
    .ciphers                 string, r/o
    .preferServerCiphers     boolean, r/o
    .sessionTimeout          number, r/o — seconds
    .sessionTickets          boolean, r/o
    .verifyDepth             number, r/o
    .verify                  {mode, depth} | null, r/o
    .clientCertificate       string, r/o — CA cert path
    .trustedCertificate      string, r/o
    .ecdhCurve               string, r/o
    .dhparam                 string, r/o

  ---
  NginxGzip
    .enable                  boolean, r/o
    .level                   number, r/o — 1–9
    .minLength               number, r/o — bytes
    .vary                    boolean, r/o
    .buffers[]               [{size, count}], r/o
    .httpVersion             string, r/o — "1.0"|"1.1"
    .proxied[]               string[], r/o — conditions

  NginxHeaders
    .expires                 string, r/o
    .expiresTime             number, r/o — epoch seconds
    .headersInherit          boolean, r/o
    .trailersInherit         boolean, r/o
    .addHeaders[]            [{field, value, always?}, ...], r/o
    .addTrailers[]           [{field, value}, ...], r/o

  NginxRewrite
    .log                     boolean, r/o
    .uninitializedVariableWarn  boolean, r/o
    .stackSize               number, r/o
    .hasRules                boolean, r/o

  NginxAccess
    .rules[]                 [{allow|deny, address}, ...], r/o — IPv4
    .rules6[]                [{allow|deny, address}, ...], r/o — IPv6
    .rulesUnix[]             [{allow|deny}, ...], r/o — Unix sockets

  NginxAuth
    .realm                   string, r/o
    .userFile                string, r/o

  ---
  NginxLimitReq
    .limits[]                [{zone, burst, nodelay?}, ...], r/o
    .logLevel                string, r/o
    .delayLogLevel           string, r/o
    .statusCode              number, r/o
    .dryRun                  boolean, r/o

  NginxLimitConn
    .limits[]                [{zone, connections}, ...], r/o
    .logLevel                string, r/o
    .statusCode              number, r/o
    .dryRun                  boolean, r/o

  ---
  NginxFastCGI
    .pass                    string, r/o
    .index                   string, r/o
    .keepConn                boolean, r/o
    .connectTimeout          number, r/o — ms
    .sendTimeout             number, r/o — ms
    .readTimeout             number, r/o — ms
    .buffering               boolean, r/o
    .requestBuffering        boolean, r/o
    .interceptErrors         boolean, r/o
    .params{}                object, r/o — fastcgi_param key-value pairs
    .catchStderr             string | null, r/o

  NginxLog
    .off                     boolean, r/o
    .logs[]                  [{name, format, level?}, ...], r/o

  NginxRealIP
    .header                  string, r/o — header name
    .recursive               boolean, r/o
    .from[]                  string[], r/o — CIDR/IP strings

  NginxCharset
    .charset                 string, r/o
    .sourceCharset           string, r/o
    .overrideCharset         boolean, r/o

  ---
  NginxSubFilter
    .pairs[]                 [{match, replacement}, ...], r/o
    .once                    boolean, r/o
    .lastModified            boolean, r/o

  NginxAutoindex
    .enable                  boolean, r/o
    .format                  string, r/o — "html"|"json"|"xml"
    .localtime               boolean, r/o
    .exactSize               boolean, r/o

  NginxReferer
    .noReferer               boolean, r/o
    .blockedReferer          boolean, r/o
    .serverNames             boolean, r/o — server_names flag
    .hashMaxSize             number, r/o
    .hashBucketSize          number, r/o

  NginxDav
    .methods[]               string[], r/o — e.g. ["PUT","DELETE","MKCOL"]
    .access                  number, r/o — permission bits
    .minDeleteDepth          number, r/o
    .createFullPutPath       boolean, r/o

  NginxSsi
    .enable                  boolean, r/o
    .silentErrors            boolean, r/o
    .ignoreRecycledBuffers   boolean, r/o
    .lastModified            boolean, r/o
    .minFileChunk            number, r/o — bytes
    .valueLen                number, r/o — bytes

  NginxUserid
    .enable                  string, r/o — "off"|"on"|"v1"|"v2"|"log"
    .name                    string, r/o — cookie name
    .domain                  string, r/o
    .path                    string, r/o
    .p3p                     string, r/o
    .expires                 number, r/o — seconds
    .mark                    string, r/o — single char or ""

  ---
  NginxAddition
    .addBeforeBody           string, r/o — URI
    .addAfterBody            string, r/o — URI

  NginxGunzip
    .enable                  boolean, r/o
    .buffers                 number, r/o — buffer size bytes

  NginxSlice
    .size                    number, r/o — bytes per slice

  NginxImageFilter
    .action                  string, r/o — "off"|"resize"|"crop"|"rotate"|"quality"|"sharpen"
    .width                   number, r/o — pixels
    .height                  number, r/o — pixels
    .angle                   number, r/o — 0|90|180|270
    .jpegQuality             number, r/o — 1–100
    .webpQuality             number, r/o — 1–100
    .sharpen                 number, r/o — 0–100
    .transparency            boolean, r/o
    .interlace               boolean, r/o
    .bufferSize              number, r/o — bytes

  NginxXslt
    .lastModified            boolean, r/o
    .sheetsCount             number, r/o
    .params{}                object, r/o — XSLT parameter key-value pairs

  NginxSecureLink
    .secret                  string, r/o
    .variable                string, r/o — nginx variable name
    .md5                     boolean, r/o

  NginxMp4
    .bufferSize              number, r/o — bytes
    .maxBufferSize           number, r/o — bytes
    .startKeyFrame           boolean, r/o

  NginxRandomIndex
    .enable                  boolean, r/o

  NginxAuthRequest
    .uri                     string, r/o — auth subrequest URI

  NginxGzipStatic
    .enable                  boolean, r/o — "off"|"on"|"always"

  NginxMemcached
    .connectTimeout          number, r/o — ms
    .sendTimeout             number, r/o — ms
    .readTimeout             number, r/o — ms
    .bufferSize              number, r/o — bytes
    .gzipFlag                number, r/o — flags bit

  NginxScgi
    .connectTimeout          number, r/o — ms
    .sendTimeout             number, r/o — ms
    .readTimeout             number, r/o — ms
    .bufferSize              number, r/o — bytes

  NginxUwsgi
    .connectTimeout          number, r/o — ms
    .sendTimeout             number, r/o — ms
    .readTimeout             number, r/o — ms
    .bufferSize              number, r/o — bytes
    .modifier1               number, r/o
    .modifier2               number, r/o

  NginxMirror
    .uris[]                  string[], r/o — mirror URIs
    .requestBody             boolean, r/o

  ---
  NginxUpstream
    .name                    string, r/o
    .zone                    string | null, r/o — shm zone name
    .peers[]                 NginxRRPeer[]
    .addPeer(addr[, opts])   function — opts: {weight?, backup?}
    .removePeer(addr)        function

  NginxPeer (config-phase)
    .address                 string, r/o
    .weight                  number, r/w
    .maxFails                number, r/w
    .failTimeout             number, r/w — seconds
    .maxConns                number, r/w
    .down                    boolean, r/w
    .backup                  boolean, r/o

  NginxRRPeer (runtime)
    .address                 string, r/o
    .server                  string, r/o — configured server address
    .weight                  number, r/w
    .maxFails                number, r/w
    .failTimeout             number, r/w — seconds
    .maxConns                number, r/w
    .down                    boolean, r/w
    .backup                  boolean, r/o
    .conns                   number, r/o — active connections
    .fails                   number, r/o — fail counter
