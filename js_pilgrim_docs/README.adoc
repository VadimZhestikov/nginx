= JS-Pilgrim Architecture
:toc: left
:toc-title: Contents
:toclevels: 2
:sectnums:
:revdate: 2026-03-30

JS-Pilgrim is the codename for the full-proxy programmable layer built on top of
the JS COM foundation.  The goal is to give JavaScript complete control over every
stage of connection and request processing — from raw TCP accept through to upstream
response body — with nginx's existing phase engine treated as one pluggable layer
rather than the fixed centre.

== Design Principles

=== Full proxy framing

Every incoming connection passes through a stack of independently programmable
layers:

....
Client TCP
  ↓
[L4 JS filters]          ← raw Buffer async-generator streams, both directions
  ↓
[Protocol detection]     ← nginx stream / http selection
  ↓
[L7 JS hooks]            ← req / res objects, async hooks before/after phases
  ↓
[nginx phase engine]     ← existing modules (proxy, FastCGI, static, …)
  ↓
[Upstream JS filters]    ← modify what goes to / comes from backend
  ↓
Backend TCP
....

A layer that does nothing passes through automatically.  A layer that calls
`req.respond()` (or equivalent) short-circuits everything below it.  This is the
BIG-IP iRules / HUD filter model: the full proxy gives independent client-side and
server-side streams with hook points at every protocol boundary.

=== Async generators as the layer primitive

Async generators compose naturally into transform pipelines:

[source,javascript]
----
async function* gzipExpand(upstream) {
    const dec = new DecompressStream();
    for await (const chunk of upstream) {
        yield* dec.write(chunk);
    }
    yield* dec.flush();
}

location.addBodyFilter(gzipExpand);
----

The three primitives:

[cols="1,3"]
|===
| `yield chunk`       | passthrough (observe and forward unchanged)
| `yield otherChunk`  | replace (emit different content)
| `return` / `throw`  | cancel / error
|===

=== Module packaging

Packages are directories with a `package.json` (`ngxjs` key) and an `index.js`
entry point.  Installed at runtime with `nginx.use(name, config)` — no reload
required.

=== Admin shell

A JS COM application that uses `nginx.repl` and WebSocket to expose a
browser-based admin UI.  Each installed package contributes a panel (analogous to
a browser DevTools tab).

== Implementation Steps

[cols="1,2,4", options="header"]
|===
| Step | Name | Description

| P1 ✓
| <<p1-location-hooks.adoc#,Location hooks>>
| `location.addHook(fn)` — async pre-content hooks on a location.  Sync and async
  (Promise-returning) hooks; cancel by calling `req.respond()`.

| P2 ✓
| <<p2-scope-hooks.adoc#,Server & global hooks>>
| `server.addHook(fn)`, `nginx.http.addHook(fn)` — hooks at higher scopes
  via the nginx access phase; fire for every matching request regardless of
  content handler.

| P3 ✓
| <<p3-response-hooks.adoc#,Response hooks>>
| `location.addResponseHook(fn)` — fires after upstream selects response
  headers, before they are sent to the client.  Modify status / headers.

| P4 ✓
| <<p4-accept-hook.adoc#,Accept hook>>
| `listener.on('accept', fn(conn))` — TCP connection level hook.
  `conn.remoteAddr`, `conn.remotePort`, `conn.reject()`.

| P5 ✓
| <<p5-gen-filter.adoc#,Generator body filter>>
| Upgrade `addBodyFilter` to accept async generator functions
  (`async function*(chunks, req)`).  `chunks` is a single-element iterable
  `[wholeBodyString]`; iterate with `for await`.  Generator yields zero or
  more output chunks and may `await` between yields.

| P6 ✓
| <<p6-l4-filter.adoc#,L4 data filter>>
| `listener.addL4Filter(asyncGenFn)` — raw TCP byte-stream filter on
  JS-managed listeners.  Client→server direction: intercepts bytes before
  HTTP parsing.  Generator receives `Uint8Array`, yields transformed bytes.

| P7 ✓
| <<p7-module.adoc#,Module packaging>>
| `nginx.use(path, config?)` / `nginx.install(plugin, config?)` API.
  Directory-based packages with `package.json` (`ngxjs.main` entry),
  inline function/object plugins, `nginx.pluginConfig` config injection.

| P8 ✓
| <<p8-async-hooks.adoc#,Async P2/P3 hooks>>
| `nginx.http.addHook(async fn)` / `server.addHook(async fn)` — server-
  and global-scope hooks that may `await` inside the body.  Request is
  suspended (count++) while the Promise is pending; resumed when it
  settles.  Throwing rejects with 500; `req.respond()` short-circuits the
  content handler exactly as in the sync case.

| P9 ✓
| <<p9-next.adoc#,next() / middleware wrapping>>
| All hook callbacks now receive a second argument `next`.  Calling
  `next()` runs the remaining hooks and returns a Promise; code after
  `await next()` in a P1/P2 hook executes before the content handler.
  Hooks that do not call `next()` auto-advance (backward-compatible).
  Implemented as a Koa-style JS IIFE (`__ngx_hook_chain__`) evaluated once
  per context.  `req.responded` (read-only) bridges JS/C for short-circuit
  detection.  `ngx_js_run_chain` replaces the old per-hook C loop.

| P10 ✓
| <<p10-ctx.adoc#,req.ctx — persistent per-request context>>
| `req.ctx` — a plain JS object tied to the nginx request pool, not to
  the JS req wrapper.  Survives async suspend/resume (wrapper replaced),
  and spans the P2-access → P1-content phase boundary.  Lazily created
  on first access; freed via pool cleanup when the request ends.
  Implemented as a C-level `JSValue ctx_obj` slot in `ngx_js_req_ctx_t`
  with a `ngx_pool_cleanup_add` finalizer.

| P11 ✓
| <<p11-shared.adoc#,nginx.shared — cross-worker key/value store>>
| `nginx.shared.get/set/delete/keys/incr` — flat fixed-size shared-memory
  zone (256 entries, ~160 kB) protected by `ngx_spinlock`.  Available in
  all request-phase handlers and timer callbacks.  Zone is only mapped when
  at least one `js_source` directive is present; omitted otherwise.
  For configurable capacity or SharedWorker interop see
  `js_pilgrim_modules/shared_string_map.js`.

| P12 ✓
| <<p12-l4-stream.adoc#,L4 full-stream + async>>
| Upgrade `listener.addL4Filter` to a true streaming async generator:
  `async function*(source)` with `for await (const chunk of source)`.
  `await` is permitted inside the loop.  Multiple TCP segments feed the
  same live generator via a JS async-iterable source factory.  Per-connection
  `ngx_js_l4_pending_t` tracks the live generator; `ngx_js_l4_async_check`
  resumes parked connections from the event loop.

| P13 ✓
| <<p13-l4-send.adoc#,L4 server→client direction>>
| `listener.addL4SendFilter(asyncGenFn)` — intercepts raw outbound bytes
  after HTTP processing, before they reach the client socket.  Hooks into
  `c->send` / `c->send_chain` via pool-cleanup type-tagged state.  EOF
  delivered lazily inside collect loop after first yield.

| P14 ✓
| <<p14-upstream-filter.adoc#,Upstream JS filters>>
| `location.addUpstreamFilter(asyncGenFn)` (response) and
  `location.addUpstreamRequestFilter(asyncGenFn)` (request body) — JS
  generators hooked into `ngx_http_upstream_t` input/output filter chain.
  Completes the top-level architecture diagram layer that P5 covers only
  at HTTP level.  Request-body count balance: `r->main->count--` before
  resuming phases prevents "open socket" hangs.

| P15 ✓
| <<p15-multi-filter.adoc#,P5 multi-filter composition>>
| Allow multiple `addBodyFilter` calls on the same location; each
  filter's output becomes the next filter's input.  `body_filters`
  is a priority-ordered `ngx_array_t`; the runner threads `body_val`
  through all entries (GENERATOR, WB_SYNC, WB_ASYNC, STREAM_*).
  No code changes — structurally complete since P5.

| P16 ✓
| <<p16-hot-reload.adoc#,Post-fork plugin loading (nginx.use hot reload)>>
| `nginx.use(path, config)` callable from a worker request handler —
  loads the plugin locally then sends `NGX_CMD_JS_USE_PLUGIN` to master,
  which forwards `NGX_CMD_JS_LOAD_PLUGIN` to every other worker.
  `ngx_js_load_plugin` is the shared helper; `ngx_js_channel_send`
  promoted from static for reuse.

| P17 ✓
| <<p17-server-accept.adoc#,Standard listen socket support for P4/P6>>
| `server.on('accept', fn)`, `server.addL4Filter(fn)`, and
  `server.addL4SendFilter(fn)` on the existing `NginxServer` COM object,
  so accept hooks and L4 filters work on sockets declared with the `listen`
  directive — not only JS-created listeners.

| P18 ✓
| <<p18-registry.adoc#,Online package registry for nginx.use>>
| `nginx.use('vendor/plugin@1.2.3', config)` — resolve from a central
  registry.  `ngx_js_is_registry_ref` detects `vendor/plugin[@version]`
  references; `ngx_js_resolve_registry_pkg` maps them to
  `$NGXJS_CACHE/packages/vendor/plugin/version/`.  CLI tool `tools/ngxjs`
  handles download, checksum, extraction; JS runtime stays network-free.

| P19 ✓
| <<p19-admin-shell.adoc#,Admin shell>>
| Browser-based admin UI using `nginx.repl` and WebSocket (JSON-RPC 2.0).
  Classic shell: four panels (Plugins, Shared State, Eval, Upstreams).
  Admin UI v2: live `nginx.*` object tree browser + per-worker REPL with
  cross-worker eval relay via `repl-relay.js` SharedWorker (`?w=N`
  targeting).  Built on two new primitives: `nginx.repl.listenRaw(fd,
  onData)` (binary chunk delivery) and `nginx.repl._writeFdRaw(fd,
  Uint8Array)` (raw byte write).  `nginx.plugins` array tracks all
  `nginx.use()` loads.
|===

== Hook Cancellation Model

At every layer the same three-outcome model applies:

[source,javascript]
----
location.addHook(async function(req) {
    // 1. Cancel — respond immediately, skip all remaining hooks + handler
    if (!req.headers['authorization']) {
        req.respond(401, {}, 'Unauthorized\n');
        return;
    }

    // 2. Continue — return without responding; next hook (or handler) runs
});
----

The chain advances automatically unless `req.respond()` is called.  P9 also
adds a `next()` argument for Koa-style middleware wrapping — code after
`await next()` executes as a post-handler epilogue:

[source,javascript]
----
nginx.http.addHook(async function(req, next) {
    const start = Date.now();
    await next();                      // run remaining hooks + content handler
    const ms = Date.now() - start;
    // headers not yet sent — safe to add
    req.setHeader('x-duration-ms', String(ms));
});
----

Hooks that do not call `next()` auto-advance (backward-compatible).

== Technical Implementation Reference

Detailed internal documentation lives in `js_dom_doc_tech/`.  Each document
covers one subsystem, ordered from high-level design decisions down to
data-structure layouts and wire formats.

[cols="3,4"]
|===
| Document | Contents

| link:../js_dom_doc_tech/01_architecture.md[01_architecture.md]
| Full-proxy seven-layer stack; the P-series feature map.

| link:../js_dom_doc_tech/02_design_decisions.md[02_design_decisions.md]
| The five major architectural decisions — COW fork, async-generator streams,
  Koa middleware, `req.ctx`, memfd SABs — with rejected alternatives and
  consequences.

| link:../js_dom_doc_tech/03_request_lifecycle.md[03_request_lifecycle.md]
| P1/P2/P3/P9 phase hooks, async suspension and resume, hook-chain synthesis,
  `ngx_js_async_ctx_t` internals.

| link:../js_dom_doc_tech/04_filter_pipeline.md[04_filter_pipeline.md]
| The five body-filter execution modes (WB_SYNC, WB_ASYNC, STREAM_SYNC,
  STREAM_ASYNC, GENERATOR), multi-filter composition (P5/P15).

| link:../js_dom_doc_tech/05_com_layer.md[05_com_layer.md]
| `nginx` COM object tree — class ID system, `NginxServer`, `NginxLocation`,
  `NginxUpstream`, `nginx.shared`, `nginx.broadcast`, module COM wrappers.

| link:../js_dom_doc_tech/06_workers_concurrency.md[06_workers_concurrency.md]
| COW fork model, `ngx_js_worker_t`, JS Worker threads, SharedWorker pthread
  architecture, manager thread, Atomics.wait/notify bridge.

| link:../js_dom_doc_tech/07_sab_shared_memory.md[07_sab_shared_memory.md]
| Pre-fork vs post-fork `SharedArrayBuffer`, `memfd_create`, `ngx_js_sab_fd_table`,
  full lifecycle walkthrough, `channel_patch_va`.

| link:../js_dom_doc_tech/08_l4_pipeline.md[08_l4_pipeline.md]
| P4 accept hooks, P6 inbound and P13 outbound async-generator TCP filters,
  `NginxSocket`, `NginxHttpListener`, `NginxConnection`.

| link:../js_dom_doc_tech/09_plugin_system.md[09_plugin_system.md]
| `nginx.use` / `nginx.install` / registry resolution — P7 init-time, P16
  post-fork hot-reload, P18 `vendor/pkg@semver` packages.

| link:../js_dom_doc_tech/10_admin_shell.md[10_admin_shell.md]
| P19 admin shell: `nginx.repl`, per-worker REPL relay via SharedWorker,
  WebSocket handshake in JS, JSON-RPC 2.0 dispatch, `admin_UI_demo_v2`.

| link:../js_dom_doc_tech/11_data_structures.md[11_data_structures.md]
| Key C struct layouts with field-level ownership and lifetime annotations:
  `ngx_js_conf_t`, `ngx_js_worker_t`, `ngx_js_req_ctx_t`, `ngx_js_sw_state_t`,
  `ngx_js_sab_hdr_t`, and more.

| link:../js_dom_doc_tech/12_protocols.md[12_protocols.md]
| Wire formats: SharedWorker channel (16-byte header + SCM_RIGHTS), manager
  thread protocol, broadcast protocol, WebSocket framing, JSON-RPC 2.0,
  SAB header memory layout.
|===

== Source Layout

[cols="2,4"]
|===
| File | Contents

| `src/js/ngx_js.h`
| Shared structs: `ngx_js_conf_t`, `ngx_js_loc_conf_t`, `ngx_js_req_ctx_t`,
  `ngx_js_async_ctx_t`, `ngx_js_worker_t`.  Hook-chain, filter, and
  upstream-filter field declarations.

| `src/js/ngx_js_module.c`
| NGINX module lifecycle: `init_conf` (runtime + COM setup, JS eval),
  `init_process` (worker inheritance), `exit_process`, `exit_master`.
  `js_source` directive.  `ngx_js_read_file`, `ngx_js_eval_module`.

| `src/js/ngx_js_com.c`
| Root `nginx` COM object: `version`, `cpu_count`, `log`, `cycle`,
  `setTimeout`, `broadcast`, `shared`, `plugins`, `set`/`get`/`settable`,
  `nginx.use` (P7/P16/P18/P19), `nginx.install` (P7).
  P18: `ngx_js_is_registry_ref`, `ngx_js_resolve_cache_dir`,
  `ngx_js_resolve_registry_pkg`.
  P19: `nginx.plugins` array (appended by `ngx_js_use` after each load).

| `src/js/ngx_js_com_http.c`
| `nginx.http`: servers, locations, upstreams.  `addHook` (P1/P2),
  `addResponseHook` (P3), `addBodyFilter` (P5/P15),
  `addUpstreamFilter` / `addUpstreamRequestFilter` (P14),
  `req.pass` (P16), `req.bodyPreread` / `req.bodyChunks()` (streaming).
  Hook chain runner `ngx_js_run_chain` (P9).

| `src/js/ngx_js_http_module.c`
| HTTP content handler, async resume (`ngx_js_async_check`), body
  buffering, `req.ctx` pool-cleanup slot (P10), `req.responded` (P9).

| `src/js/ngx_js_listener.c`
| `NginxListener`: `on('accept', fn)` (P4), `addL4Filter` (P6/P12),
  `addL4SendFilter` (P13).  Async L4 generator drive loop.

| `src/js/ngx_js_com_upstream.c`
| `nginx.http.upstreams[]`: peer weight/down/maxFails etc., RR reload.

| `src/js/ngx_js_sw.c` / `ngx_js_sw.h`
| `SharedWorker`: master pthread, per-worker AF_UNIX channels, memfd SAB
  transfer, dynamic creation, Atomics.wait/notify bridge.

| `src/js/ngx_js_worker.h`
| `ngx_js_worker_t` struct definition (rt, ctx, channel, pending, etc.).

| `src/js/ngx_js_repl.c`
| `nginx.repl` object: `_writeFd`, `_writeFdRaw` (P19), `eval`, `attach`,
  `detach`, `listen`, `listenRaw` (P19).  `ngx_js_repl_conn_t` tracks
  per-connection state with a `raw:1` flag for binary delivery mode.

| `t/js_pilgrim_p*.t`
| Per-step test suites (P1–P19).  Run with
  `TEST_NGINX_BINARY=objs/nginx prove -v t/js_pilgrim_p*.t`.
|===

== Reference Applications

[cols="2,4"]
|===
| Directory | Description

| `js_pilgrim_apps/zero-trust-gateway/`
| JWT authentication gateway with response-header scrubbing, HTML body
  injection, and health-check bypass.  Tests: `t/gateway.t`,
  `t/gateway_edge.t`.

| `js_pilgrim_apps/body-json-based-routing/`
| Streaming JSON body scanner that routes requests to different backends
  based on fields in the request body — without buffering the full body.
  Tests: `t/router.t`, `t/router_edge.t`.

| `js_com_apps/admin_snapshot_rollback/`
| Dynamic upstream weight management with ops-list snapshots, rollback,
  pinning, and a REST admin API.  Tests: `t/js_admin_base.t`,
  `t/js_admin_api.t`, `t/js_admin_snapshot.t`, `t/js_admin_rollback.t`,
  `t/js_admin_compact.t`, `t/js_admin_struct.t`, `t/js_admin_edge.t`.

| `js_pilgrim_apps/admin-shell/`
| WebSocket JSON-RPC backend (P19).  `admin-shell.js` handles the HTTP
  upgrade, dispatches JSON-RPC 2.0 methods, and relays cross-worker evals
  through `repl-relay.js`.  Classic SPA (`static/index.html`) with
  Plugins, Shared State, Eval, and Upstreams panels.
  Test: `t/js_pilgrim_p19_admin_shell.t`.

| `js_pilgrim_apps/admin_UI_demo_v2/`
| Full-featured demo app built on the admin-shell backend.  Left panel:
  live `nginx.*` object tree browser — expandable view of any JS
  expression.  Right panel: per-worker REPL — one tab per worker, each
  pinned to its worker via `?w=N`.  Includes two plugins (`banner`,
  `metrics`) and three upstream groups.
  Run: `objs/nginx -p . -c js_pilgrim_apps/admin_UI_demo_v2/nginx.conf`
|===

== Pilgrim Modules

`js_pilgrim_modules/` contains reusable JS-only building blocks that can be
imported from any `js_source` script.  All modules are pure JavaScript with
no C dependencies — they rely only on SAB, Atomics, and standard QuickJS
globals.

[cols="2,4"]
|===
| Module | Description

| `shared_string_map.js`
| `SharedStringMap(capacity, maxKeyBytes, maxValBytes[, sab])` — synchronous
  cross-worker string key-value store backed by a pre-fork
  `SharedArrayBuffer`.  Open-addressing hash table with FNV-1a hash, linear
  probing, tombstone deletion, and an `Atomics.compareExchange` spinlock.
  API mirrors `nginx.shared` (`get`, `set`, `delete`, `has`, `size`, `clear`)
  with configurable capacity and slot sizes.  Pass `.sab` to a `SharedWorker`
  via `postMessage` to share the same map across worker-process and
  SharedWorker contexts.
  See also: <<p11-shared.adoc#,P11 — nginx.shared>>.
|===
