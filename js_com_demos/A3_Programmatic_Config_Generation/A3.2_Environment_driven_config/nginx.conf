worker_processes  1;
pid               logs/nginx.pid;
error_log         logs/error.log  info;

# js_preprocess reads APP_ENV at parse time and generates the appropriate
# server configuration (dev on port 8118, prod on port 8119).
js_preprocess     gen.js;

events {
    worker_connections  256;
}
