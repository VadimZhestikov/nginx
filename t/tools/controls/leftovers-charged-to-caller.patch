diff --git a/src/js/ngx_js_module.c b/src/js/ngx_js_module.c
index 2c2133d60..e1616b380 100644
--- a/src/js/ngx_js_module.c
+++ b/src/js/ngx_js_module.c
@@ -4549,7 +4549,7 @@ ngx_js_comcon_invoke_confined(JSContext *hctx, JSValueConst this_val,
      * this fragment's job budget, deadline, allowance or rejection report.  See
      * ngx_js_comcon_drain_leftovers() for why they run as nobody.
      */
-    ngx_js_comcon_drain_leftovers(jcf);
+    if (0) { ngx_js_comcon_drain_leftovers(jcf); }   /* the control: leftovers stay for the caller */
 
     /*
      * F2's LEAK HALF: the cap on what this fragment may hold ACROSS calls,
