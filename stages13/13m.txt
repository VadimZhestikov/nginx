● Stage 13l done. 664 tests passing.

  ---
  Stage 13m proposal — slice (ngx_http_slice_filter_module)

  Splits requests into subrequests for byte-range caching. Built-in (no --with-* needed). location.slice:

  ┌──────────┬────────┬───────────────────────────────────────┐
  │ Property │  Type  │               Directive               │
  ├──────────┼────────┼───────────────────────────────────────┤
  │ size     │ number │ slice (bytes per subrequest, 0 = off) │
  └──────────┴────────┴───────────────────────────────────────┘

  Tiny struct — just one field. Shall I proceed with Stage 13m (slice)?
