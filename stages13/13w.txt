● Stage 13v done. 783/783 passing.

  ---
  Stage 13w proposal — nginx.http.servers[i].ssl expansion

  Currently nginx.http.servers[i].ssl (Stage 6) exposes the SSL server config. We have plenty of per-location modules covered now. Let me look at what's left at the location level versus moving to
  other interesting areas.

  Remaining notable per-location modules not yet exposed:
  - ngx_http_mirror_module — ngx_http_mirror_loc_conf_t has mirror (array of URIs) and mirror_request_body (flag)
  - ngx_http_try_files_module — no separate loc_conf; it stores in ngx_http_core_loc_conf_t.try_files
  - ngx_http_split_clients_module — no loc_conf
  - ngx_http_geo_module — no loc_conf

  Stage 13w proposal: location.mirror (NginxMirror)

  ngx_http_mirror_module has:
  typedef struct {
      ngx_array_t  *mirror;        /* array of ngx_str_t URIs */
      ngx_flag_t    mirror_request_body;
  } ngx_http_mirror_loc_conf_t;

  Expose: uris[] (array of mirror target URIs), requestBody (boolean).

  Shall I proceed?
