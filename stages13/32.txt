● Stage 31 done. Proposing Stage 32:

  ---
  Stage 32 — nginx.http.servers[i].variables (server-scoped variable registry)

  Or alternatively, something more impactful for real-world use:

  Stage 32 — r.fetchUpstream(url[, opts]) — async HTTP fetch to an upstream

  A Promise-based HTTP client that lets JS handlers make outbound HTTP requests to backend services:

  async function handler(r) {
      const res = await r.fetchUpstream('http://127.0.0.1:8081/api');
      r.respond(200, {'content-type': 'application/json'}, res.body);
  }

  This would use nginx's ngx_http_subrequest mechanism (same as r.subrequest()) but with a full URL instead of an internal path, or use a dedicated upstream connection.

  Actually, r.subrequest() already covers the internal proxy case. A more useful Stage 32 might be:

  Stage 32 — nginx.http.addVariable(name) + r.variables[name] write

  Allow JS scripts to register new nginx variables at init_conf time, then read/write them from request handlers (enabling cross-location data passing via nginx variables).

  Or simply:

  Stage 32 — r.sleep(ms) — async timer in request handler

  async function handler(r) {
      await r.sleep(100);   // pause 100ms, returns to event loop
      r.respond(200, {}, 'delayed');
  }

  Uses the existing ngx_js_timer_t mechanism from nginx.setTimeout but scoped to the request.

  Which direction?


all one by one in separate commits and tests