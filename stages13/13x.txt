● Stage 13w done. 797/797 passing.

  ---
  Stage 13x proposal — location.try_files array

  try_files is stored directly in ngx_http_core_loc_conf_t.try_files — an ngx_array_t * of ngx_http_try_file_t entries (each has a name ngx_str_t). Since this is already in the core loc_conf we
  already read (no separate module), it can be added as a new plain property on NginxLocation without a sub-object.

  Expose location.tryFiles → string array of the try_files arguments (the raw strings as configured, e.g. ["$uri", "$uri/", "/index.html"]).

  This is a zero-new-class change — just a new getter on the existing NginxLocation with a JS_CGETSET_DEF (or a new magic). The source of truth is ngx_http_core_loc_conf_t which ngx_js_com_http.c
  already uses.

  Shall I proceed with location.tryFiles?
