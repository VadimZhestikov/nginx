● Stage 19 done. 945 tests, 70 files, all passing.

  ---
  Stage 20 candidates:

  Option A — r.subrequest(uri[, options]) → Promise: Issue an nginx subrequest from a JS async handler. Returns {status, headers, body}. Builds on the existing async/suspend infrastructure.

  Option B — r.sendfile(path) / static file serving from JS: Serve a static file without a separate location block.

  Option C — Response-header API: r.setHeader(name, value) sets individual response headers before finalizing. Useful for building up headers incrementally.

  Option D — nginx.upstream(name) lookup method: nginx.http.upstream("mybackend") returns the upstream by name directly, complementing the existing nginx.http.upstreams[] array.

  Option A (subrequest) is the most powerful capability — it enables JS handlers to fan out to other nginx locations (proxies, static, etc.) and compose responses. Shall I proceed with Stage 20 as
  Option A?

