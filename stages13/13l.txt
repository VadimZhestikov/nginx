 Stage 13k done. 655 tests passing.

  ---
  Stage 13l proposal — gunzip (ngx_http_gunzip_filter_module)

  Decompresses gzip-encoded upstream responses. Optional (--with-http_gunzip_module). Exposes location.gunzip:

  ┌──────────┬─────────────┬────────────────┐
  │ Property │    Type     │   Directive    │
  ├──────────┼─────────────┼────────────────┤
  │ enable   │ boolean     │ gunzip on/off  │
  ├──────────┼─────────────┼────────────────┤
  │ buffers  │ {num, size} │ gunzip_buffers │
  └──────────┴─────────────┴────────────────┘

  Small struct, straightforward. Shall I proceed with Stage 13l (gunzip)?
