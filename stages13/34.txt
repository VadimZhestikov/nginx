● Stage 33 done. Now for Stage 34. Based on the session summary, this was r.fetchUpstream(url[, opts]) — a Promise-based HTTP fetch using nginx's subrequest mechanism. Let me think about the design
  before proposing.

  The options are:
  1. Internal subrequest (r.subrequest(uri, opts)) — passes the request internally to another nginx location
  2. Full HTTP proxy fetch — nginx upstream fetch to an external URL

  Which direction did the plan go? The summary said "TBD". Let me propose concrete options:

  ---
  Stage 34 candidates:

  Option A — r.subrequest(uri[, opts])
  Fires an internal nginx subrequest to another location in the same server. Returns a Promise resolving to {status, headers, body}. Uses ngx_http_subrequest().
  Use cases: auth backend, internal API calls, proxied content injection.

  Option B — r.fetch(url[, opts])
  Full outbound HTTP fetch to an arbitrary URL using nginx's upstream mechanism. Returns a Promise. More complex (requires upstream config or direct socket), but useful for external APIs.

  Option C — r.setVar(name, value) / r.getVar(name) convenience methods
  Simple wrappers around r.variables['name'] = value for completeness.

  Recommendation: Option A (r.subrequest) is more natural in nginx's architecture and commonly needed. Option B requires substantially more infrastructure. Option C is trivial.

  What would you like for Stage 34?



A, C and B in this order with separate commits and tests

