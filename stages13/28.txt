  Stage 28 — r.variables (per-request variable access)

  Expose all nginx variables for a given request as a readable/writable JS object:

  r.variables.host        // → "localhost"
  r.variables.uri         // → "/path"
  r.variables.my_var = "x"  // set a CHANGEABLE variable

  Implementation:
  - Add magic 19 in ngx_js_request_get returning a JS Proxy (or a plain object built lazily)
  - Actually the cleanest approach: add a separate NginxRequestVariables class with a custom get/set trap; or use a plain JS getter-per-variable approach
  - Simplest: return a plain object with all variables filled at access time (lazy getter via Proxy is ideal, but QuickJS supports Proxy). Alternatively use JS_SetPropertyStr for each known variable
  (uses nginx.http.variables index map)
  - Read: ngx_http_get_indexed_variable(r, v->index) → var->data/var->len
  - Write (only if CHANGEABLE): ngx_http_variable_set_by_name or direct ngx_http_set_indexed_variable

  Tests: r.variables.host, r.variables.uri, writing a set-directive variable, reading back after write.

  Shall I proceed?
