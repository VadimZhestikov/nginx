 Stage 13r done. 727/727 passing.

  ---
  Stage 13s proposal — location.stubStatus (NginxStubStatus)

  ngx_http_stub_status_module exposes a single per-location flag:

  typedef struct {
      ngx_uint_t  enable;
  } ngx_http_stub_status_loc_conf_t;

  One property: enable (boolean) — whether stub_status is active in this location. Expose as location.stubStatus.enable.

  Shall I proceed?
