● Stage 10 complete. All 453 tests pass.

  What was implemented:

  - nginx/src/http/modules/ngx_http_rewrite_module.h — public header with ngx_http_rewrite_loc_conf_t
  - nginx/src/js/ngx_js_com_rewrite.c — NginxRewrite class exposing:
    - log — rewrite_log on/off
    - uninitializedVariableWarn — uninitialized_variable_warn on/off
    - stackSize — script engine stack depth (default: 10)
    - hasRules — true when any rewrite/return/set/if rules are compiled in
  - Test: t/js_com_rewrite.t — 10 explicit + 2 auto = 12 tests

  Ready for Stage 11 when you are.

✻ Churned for 3m 52s
