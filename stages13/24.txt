  Stage 24 proposal — r.respond() header object expansion

  Currently r.respond(status, headers, body) only handles "content-type" specially. Expand it to honour any arbitrary response header key/value from the headers object, writing them via ngx_list_push
   into r->headers_out.headers.

  This lets JS handlers set Cache-Control, X-Custom-Header, Location, etc. without needing special-case C code per header.

  Test: t/js_com_respond_headers.t — verify arbitrary headers appear in the response (Cache-Control, X-Custom, multiple headers).

  Shall I proceed?

yes!