● Stage 20 is done. Now let me propose Stage 21.

  ---
  Stage 21 proposal — r.headers (request headers object)

  Expose r.headers as a read-only plain JS object containing all incoming request headers. Each header name is lowercased (e.g. "content-type", "accept").

  Implementation: iterate r->headers_in.headers (ngx_list_t) in ngx_js_request_get (new magic case 16) and build a JS_NewObject with one string property per header.

  Test: t/js_com_request_headers.t — verify content-type, accept, host, header count.

  Shall I proceed?
