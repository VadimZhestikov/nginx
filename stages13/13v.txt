● Stage 13u done. 755/755 passing.

  ---
  Stage 13v proposal — location.scgi (NginxScgi)

  ngx_http_scgi_module has a loc_conf similar to fastcgi/memcached — upstream-based with SCGI-specific fields. Key fields in ngx_http_scgi_loc_conf_t:

  - upstream — ngx_http_upstream_conf_t (timeouts, buffer sizes)
  - params_source — SCGI params
  - index — index file string
  - store / store_access — caching to disk

  The most useful subset to expose: connectTimeout, sendTimeout, readTimeout, bufferSize.

  Alternatively, we could jump to location.uwsgi which has the same shape. Or we can do both in one pass since they're structurally near-identical.

  Proposal: do scgi and uwsgi together as Stage 13v — same pattern, both upstream-based. Each gets its own class but one commit.

  Shall I proceed with both together?
