● Stage 18 done. 939 tests, 69 files, all passing.

  ---
  Stage 19 candidates:

  Option A — r.location getter: Returns the NginxLocation object for the request's matched location — bridges the two object trees so handlers can read config of their own location.

  Option B — r.subrequest(uri[, options]) → Promise: Issue an nginx subrequest from a JS handler. Returns {status, headers, body}. Requires async handler support (already works). Most powerful but
  complex to implement.

  Option C — r.sendfile(path) / r.sendfileRange(path, start, end): Serve a file from JS — useful for dynamic file serving without a separate static location.

  Option D — Response-header API: r.setHeader(name, value) / r.addHeader(name, value) to set response headers incrementally before finalizing. Currently respond() sets them all at once from a plain
  object.

  Option A (r.location) is the most natural bridge and self-contained (no new async machinery). Shall I proceed with Stage 19 as Option A?

yes!