● Stage 13y done. 826/826 passing.

  ---
  Stage 13z proposal — wrap-up of remaining core location properties

  Several useful fields remain in ngx_http_core_loc_conf_t not yet exposed. I can add them as plain getters (zero new classes) in one final pass:

  - location.lingering — "off" / "on" / "always" (NGX_HTTP_LINGERING_OFF/ON/ALWAYS)
  - location.lingering_timeout — ms
  - location.lingering_time — ms
  - location.resolverTimeout — ms (resolver_timeout)
  - location.chunkedTransferEncoding — boolean
  - location.msieRefresh — boolean
  - location.msieBuffering — boolean
  - location.logNotFound — boolean
  - location.logSubrequest — boolean
  - location.recursive_error_pages — boolean

  That's 10 plain getters on the existing NginxLocation, all from ngx_http_core_loc_conf_t, one commit.

  Shall I proceed with all 10 as Stage 13z?
