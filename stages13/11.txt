● Stage 11 complete. All 478 tests pass.

  What was implemented:

  location.access (NginxAccess):
  - rules[] — IPv4 allow/deny: [{deny, cidr}] where cidr is "all" for 0.0.0.0/0, "1.2.3.4" for /32, "1.2.3.0/8" for subnets
  - rules6[] — IPv6 allow/deny (empty array when built without NGX_HAVE_INET6)
  - rulesUnix[] — Unix-socket allow/deny (empty without NGX_HAVE_UNIX_DOMAIN)

  location.auth (NginxAuth):
  - realm — literal string, "off" when auth_basic off, or null when unset/dynamic
  - userFile — literal path or null when unset/dynamic

  Ready for Stage 12 when you are.

✻ Brewed for 7m 42s
