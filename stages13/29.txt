  Stage 29 — r.body (request body access)

  Expose the request body as a JS string (or null if not read):

  r.body   // → string content of request body, or null if not buffered

  Implementation:
  - Magic 20 in ngx_js_request_get
  - Check r->request_body and r->request_body->bufs
  - Collect all chained bufs into a single JS string
  - Handle both in-memory bufs (buf->in_file == 0) and file-buffered bodies (return null or a truncated read for simplicity)
  - Body is only available after ngx_http_read_client_request_body() — since our handlers are content-phase, it's available if the client sent one and nginx buffered it

  Tests: POST with body → r.body returns body string; GET (no body) → r.body is null.

  Shall I proceed?

yes!!