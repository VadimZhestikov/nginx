● Stage 12 is complete. Here's a summary of what was implemented:

  Stage 12 — location.limitReq and location.limitConn

  New files:
  - nginx/src/http/modules/ngx_http_limit_req_module.h — extracts ngx_http_limit_req_conf_t + ngx_http_limit_req_limit_t
  - nginx/src/http/modules/ngx_http_limit_conn_module.h — extracts ngx_http_limit_conn_conf_t + ngx_http_limit_conn_limit_t
  - nginx/src/js/ngx_js_com_limit_req.c — NginxLimitReq class: limits[] ({zone, burst, nodelay, delay}), logLevel, delayLogLevel, statusCode, dryRun
  - nginx/src/js/ngx_js_com_limit_conn.c — NginxLimitConn class: limits[] ({zone, conn}), logLevel, statusCode, dryRun
  - t/js_com_limit_req.t — 13+2=15 tests
  - t/js_com_limit_conn.t — 11+2=13 tests

  Notable: burst and delay in the limit_req struct are stored as value × 1000 internally; the C wrapper divides by 1000 to present user-configured values.

  Both limits arrays use embedded ngx_array_t (not a pointer); no compile-time guards needed.

  Full suite: 506 tests, all passing. Ready for Stage 13 when you are.

✻ Brewed for 6m 46s
