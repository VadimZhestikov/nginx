# S1 — the attenuation vocabulary on one capability.

worker_processes 1;
error_log logs/error.log notice;
pid logs/nginx.pid;

js_source host.js;

events { worker_connections 64; }

http {
    access_log off;
    server {
        listen 127.0.0.1:8204;
        location /arms { }
    }
}
